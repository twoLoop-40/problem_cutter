# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0

# PDF handling (choose one or both)
PyMuPDF>=1.23.0          # Recommended: faster, no external deps
pdf2image>=1.16.0        # Alternative: requires poppler

# OCR (optional, for text extraction)
pytesseract>=0.3.10
easyocr>=1.7.0

# Image processing
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0

# FastAPI & Web Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database (SQLite ORM)
sqlalchemy>=2.0.0

# HTTP client (for Mathpix API)
aiohttp>=3.9.0

# UI (Phase 1)
streamlit>=1.28.0
requests>=2.31.0


